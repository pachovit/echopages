import React from 'react';
import NavBar from '../components/NavBar';

const HomePage = () => {
    return (
        <div>
            <NavBar />
            {/* Other content or an empty space */}
        </div>
    );
};

export default HomePage;